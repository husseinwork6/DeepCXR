[project]
name = "deepcxr"
version = "0.1.0"
description = "A deep learning-based framework for chest X-ray analysis."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anomalib>=2.4.1",
    "jupyter>=1.1.1",
]
